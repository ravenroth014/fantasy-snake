namespace FS_Runtimes.Controllers.UI
{
    public class LoadingStateController : AbstractUIController
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
